@model IEnumerable<BulkyBook.Models.CoverType>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-success">Cover Type List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" type="button" class="btn btn-primary btn-lg">
                <i class="bi bi-plus-square"></i> &nbsp; New CoverType
            </a>
        </div>
    </div>
</div>

<table class="table table-bordered table-striped table-secondary" style="width:100%">
    <thead>
        <tr>
            <th>
                <label class="text text-success">CoverType Name</label>
            </th>
            <th>
                <label class="text text-success">Creation Date</label>
            </th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>    
                <td width="60%">
                    <label class="text text-success">@obj.Name</label>
                </td>
                <td width="20%">
                    <label class="text text-success">@obj.CreateDateTime</label>
                </td>
                <td width="10%" style="vertical-align:middle; text-align:center">
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="CoverType" asp-action="Edit" asp-route-id="@obj.Id"
                        class="btn btn-primary mx-2"><i class="bi bi-pencil-square"></i> Edit</a>
                    </div>
                </td>
                <td width="10%" style="vertical-align:middle; text-align:center">
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="CoverType" asp-action="Delete" asp-route-id="@obj.Id"
                        class="btn btn-danger mx-2"><i class="bi bi-trash"></i> Delete</a>
                    </div>
               </td> 
            </tr>
        }
    </tbody>
</table>